import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const fixtureText = () => {
  return (
    <View>
      <Text>fixtureText</Text>
    </View>
  )
}

export default fixtureText

const styles = StyleSheet.create({})